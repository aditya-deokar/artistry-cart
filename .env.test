# ─── E2E Test Environment ───
# Used by E2E tests against real running services.
# Start test infra:  docker compose -f docker-compose.test.yml up -d

DATABASE_URL=mongodb://localhost:27018/artistry_cart_test
REDIS_URL=redis://localhost:6380

# Auth secrets (test-only values)
ACCESS_TOKEN_SECRET=test-e2e-access-secret
REFRESH_TOKEN_SECRET=test-e2e-refresh-secret
ACTIVATION_TOKEN_SECRET=test-e2e-activation-secret

# Stripe test keys (use Stripe test mode keys)
STRIPE_SECRETE_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_test_placeholder
STRIPE_CLIENT_ID=ca_test_placeholder

# Service base URLs (default ports)
AUTH_SERVICE_URL=http://localhost:6001
PRODUCT_SERVICE_URL=http://localhost:6002
ORDER_SERVICE_URL=http://localhost:6004
RECOMMENDATION_SERVICE_URL=http://localhost:6005
API_GATEWAY_URL=http://localhost:8080

# ImageKit (test mode)
IMAGEKIT_PUBLIC_KEY=test_public_key
IMAGEKIT_PRIVATE_KEY=test_private_key
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/test

# OAuth (test mode — redirect URIs should point to localhost)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
GITHUB_CLIENT_ID=test-github-client-id
GITHUB_CLIENT_SECRET=test-github-client-secret
FACEBOOK_CLIENT_ID=test-facebook-client-id
FACEBOOK_CLIENT_SECRET=test-facebook-client-secret

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Email (test mode — use Ethereal or similar)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=test@ethereal.email
SMTP_PASS=test-password
