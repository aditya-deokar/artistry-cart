apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: recommendation-service
  template:
    metadata:
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "6005"
        prometheus.io/scrape: "true"
      labels:
        app.kubernetes.io/name: recommendation-service
        app.kubernetes.io/component: api
    spec:
      containers:
        - name: recommendation-service
          image: ghcr.io/your-org/artistry-cart-recommendation-service:master
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 6005
          env:
            - name: NODE_ENV
              value: production
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: "6005"
          envFrom:
            - configMapRef:
                name: artistry-cart-platform-config
            - secretRef:
                name: artistry-cart-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 300m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
