apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
spec:
  selector:
    app.kubernetes.io/name: recommendation-service
  ports:
    - name: http
      port: 6005
      targetPort: http
