# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @artistry-cart/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t artistry-cart-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system artistry-cart-auth-service && \
          adduser --system -G artistry-cart-auth-service artistry-cart-auth-service

COPY apps/auth-service/dist artistry-cart-auth-service/
COPY apps/auth-service/package.json artistry-cart-auth-service/
RUN chown -R artistry-cart-auth-service:artistry-cart-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix artistry-cart-auth-service --omit=dev -f install

CMD [ "node", "artistry-cart-auth-service" ]
